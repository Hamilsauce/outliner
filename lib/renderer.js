class Renderer {
  constructor() {}

  static process() {}
}

const listView = (items) => {
  return ``;
}

const listNode = (nodeModel) => {
  return ``;
}

const graphView = (graphModel) => {
  return ``;
}

const graphNode = () => {}

export const Render = {
  listView,
  listNode,
  graphView,
  graphNode,
}